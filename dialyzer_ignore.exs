[
  ~r/Hex.API/,
  # temporarily ignoring the old problems so that we can keep the new code clean more easily
  ~r{lib/mix/tasks/.*},
  ~r{lib/hoplon/lockfile.ex},
  ~r{asn1rt},
  ~r{src/generated/HoplonMessages.erl}
]
